export class Tasks{ 
    name: string
    surname: string
    identity: string
    employee: string

    constructor(name,surname,identity,employee){
this.name=name;
this.surname=surname;
this.identity=identity;
this.employee=employee;

    }
}